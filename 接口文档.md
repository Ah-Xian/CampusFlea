## 校园二手交易平台 API 接口文档

**基础URL**: `http://localhost:25080`

**在线文档**: 
- Swagger UI: `http://localhost:25080/swagger-ui.html`
- OpenAPI JSON: `http://localhost:25080/v3/api-docs`

---

### 1. 用户接口 `/user`

|接口|方法|URL|参数|说明|
|---|---|---|---|---|
|用户注册|POST|`/user/register`|account, password, nickname, college(可选), grade(可选), phone(可选), wechat(可选)|注册新用户|
|用户登录|POST|`/user/login`|account, password, deviceType(可选)|登录，返回用户信息+token，实现登录互顶|
|获取用户信息|GET|`/user`|无|获取当前登录用户信息（需登录）|
|退出登录|GET|`/user/logout`|无|退出当前会话，删除设备记录（需登录）|
|更新用户信息|POST|`/user/update`|nickname(可选), college(可选), grade(可选), phone(可选), wechat(可选)|更新用户信息（需登录）|
|获取在线设备|GET|`/user/devices`|无|获取当前用户的所有在线设备（需登录）|
|踢掉其他设备|POST|`/user/kick-devices`|无|踢掉当前用户的其他登录设备（需登录）|
|管理员获取用户列表|GET|`/user/admin/list`|无|获取所有用户（需管理员）|
|管理员封禁用户|POST|`/user/{id}/ban`|reason, endAt(可选)|封禁用户（需管理员）|

### 2. 商品接口 `/goods`

|接口|方法|URL|参数|说明|
|---|---|---|---|---|
|获取商品列表|GET|`/goods`|无|获取在售商品列表|
|搜索商品|GET|`/goods/search`|keyword|搜索商品|
|获取商品详情|GET|`/goods/{id}`|无|获取商品详情，增加浏览量|
|获取商品图片|GET|`/goods/{id}/images`|无|获取商品的所有图片|
|获取商品首图|GET|`/goods/{id}/image`|无|获取商品的第一张图片|
|获取分类列表|GET|`/goods/categories`|无|获取所有商品分类|
|获取我的商品|GET|`/goods/my`|无|获取当前用户发布的商品（需登录）|
|管理员获取商品|GET|`/goods/admin/list`|无|获取所有商品（需管理员）|
|发布商品|POST|`/goods`|title, price, categoryId, description(可选), dealType(可选), meetPlace(可选)|发布商品（需登录）|
|上传文件|POST|`/goods/upload`|file|上传文件，返回URL（需登录）|
|添加商品图片|POST|`/goods/{id}/images`|imageUrl, sortOrder(可选)|为商品添加图片（需登录）|
|删除商品图片|DELETE|`/goods/images/{imageId}`|无|删除商品图片（需商品所有者）|
|更新商品状态|POST|`/goods/{id}/status`|status|更新状态（需登录）|
|举报商品|POST|`/goods/{id}/report`|reason|举报商品（需登录）|
|更新商品信息|PUT|`/goods/{id}`|title(可选), description(可选), price(可选), categoryId(可选), dealType(可选), meetPlace(可选)|更新商品信息（需登录）|

### 3. 订单接口 `/order`

|接口|方法|URL|参数|说明|
|---|---|---|---|---|
|获取我的订单|GET|`/order`|无|获取当前用户的订单列表（需登录）|
|获取订单详情|GET|`/order/{id}`|无|获取订单详情（需登录）|
|管理员获取订单|GET|`/order/admin/list`|无|获取所有订单（需管理员）|
|创建订单|POST|`/order`|goodsId, sellerId, amount, remark(可选)|创建订单（需登录）|

---

### 4. 消息接口 `/messages`（商品聊天：ProductChat + ChatMessage）

|接口|方法|URL|参数|说明|
|---|---|---|---|---|
|发送消息|POST|`/messages`|sellerId, goodsId, content|买家联系卖家，创建/更新商品聊天会话（需登录）|
|获取聊天会话列表|GET|`/messages/chats`|无|获取当前用户的所有商品聊天会话（需登录）|
|获取聊天会话详情|GET|`/messages/chats/{id}`|无|获取指定聊天会话详情（需登录）|
|获取聊天消息列表|GET|`/messages/chats/{id}/messages`|无|获取指定聊天会话的所有消息（需登录）|
|发送聊天消息|POST|`/messages/chats/{id}/messages`|content, msgType(可选), fileUrl(可选)|向指定聊天会话发送消息（需登录）|
|标记已读|POST|`/messages/chats/{id}/read`|无|标记聊天会话为已读（需登录）|

---

### 响应格式

```json
// 成功
{"code": 200, "message": "success", "data": {...}}

// 错误
{"code": 400/401/403/500, "message": "错误信息"}
```

### 枚举值

|字段|枚举值|
|---|---|
|Goods.status|ON_SALE, SOLD, OFF_SHELF, PENDING_REVIEW|
|Goods.dealType|MEET, DELIVERY, BOTH|
|Order.status|PENDING, COMPLETED, CANCELLED|
|User.role|USER, ADMIN|
|User.status|ACTIVE, BANNED|

### 注意事项

1. 需要登录的接口会在 Session 中检查 userId
2. 管理员接口会检查 userRole 是否为 ADMIN
3. categoryId 不能传字符串 "null"
4. 文件上传最大支持 10MB
5. 交易方式 dealType 可选值：MEET（见面交易）、DELIVERY（快递）、BOTH（都行）
6. 上传的文件访问路径：`http://localhost:25080/uploads/文件名`
7. 所有时间相关字段均为 Java 时间戳格式
8. 登录实现互顶功能，新登录会踢掉之前的设备
9. 登录返回 token，用于设备管理和验证身份
10. 消息功能：用户可联系商家，消息关联商品
11. 商品聊天功能：一个商品 = 一个独立聊天场景，支持发送文字、图片、文件